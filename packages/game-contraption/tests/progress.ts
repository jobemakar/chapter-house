import assert from "node:assert/strict";import {CONTRAPTION_REWARD_IDS,loadContraptionProgress} from "../src/progress";
const fresh=loadContraptionProgress({version:2,delivered:-9,board:99,cleared:[0,0,7,"x"],owned:["cc-spring-ornament"]});assert.equal(fresh.delivered,0);assert.equal(fresh.board,5);assert.deepEqual(fresh.cleared,[0]);assert.deepEqual(fresh.ownedRewardIds,[CONTRAPTION_REWARD_IDS[0]]);const legacy=loadContraptionProgress({version:1,delivered:20,mute:true});assert.equal(legacy.delivered,20);assert.equal(legacy.mute,true);assert.deepEqual(legacy.ownedRewardIds,CONTRAPTION_REWARD_IDS);
