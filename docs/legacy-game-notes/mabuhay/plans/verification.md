# Canonical migration verification — 2026-09-19

`npm ci` completed from the canonical Mabuhay root. `npm run verify` passed
strict TypeScript, rebuilt the canonical `dist/index.html` and offline playable,
executed 25 compiled-export mechanics/input/save/hazard checks plus a generated
output/notice check, and passed the Midnight Merienda archive hash/closure test.
A second rebuild produced the same
Moonlight playable SHA-256:
`E6C7B2C330679738A8E11D133B384DB94C2E8D2B1AA55C4D9EDB39CD8521A6F2`
(9,742,784 bytes). The generated JS and offline playable retain the adapted
Space Patrol MIT notice. This validates packaging and deterministic simulation;
browser interaction, visual review, physical touch devices, audio listening and
balance QA remain pending for this migration. No publication occurred.

# Completion verification — 2026-09-16

The rushed single-file draft and source-regex tests were replaced with readable TypeScript class modules: exported deterministic RunModel/SaveStore, PointerCoordinator, NightAudio, NightRenderer and browser RunGame lifecycle. Original Merienda and Site checkouts were not changed.

`npm test` rebuilds under strict TypeScript and executes real compiled exports: once-only projectile consumption with overlapping guests, current-lane toss/miss cleanup, fed departure/particles cleanup, 20-minute endless bounded simulation, harmless collisions, null/malformed/out-of-range saves plus both settings, independent simultaneous pointers/cancel and mouse steering/fire across the canvas.

Final suite: 9/9 passing, including exact 8/25/60 keepsake thresholds, both settings round-trip, malformed JSON/null and denied storage recovery. Prettier is lockfile-pinned and all TypeScript/HTML/build/test source is formatted. Final canonical artifact has two embedded PNG data URIs and no external script; SHA-256 `a13008d1f7b24d740f80a015afc96a518ffd148c546c9f6b27024c05f666abeb` (4,407,536 bytes).

Browser inspection at desktop 1280×720, landscape 844×390 and portrait 390×844: isolated generated sprites, readable night-market road, all controls retained and normalized reachable lanes unchanged. Help clock remained 4.727 across separate observations; F produced no shots while help was open. Pause clock remained 10.622 across separate observations; F produced no shots while paused. Help close recovers; fullscreen targets the whole shell, retaining header controls and instructions. This in-app browser's fullscreen screenshot initially scaled oddly; a refresh restored correct portrait screenshots and DOM bounds were correct throughout.

No physical phone/iPad touch test or speaker/headphone listening was performed. Paired-pointer behavior is covered by real input-coordinator tests, not claimed as a physical-device test. WebAudio oscillator/envelope implementation is present; audio is suspended while paused/help/hidden and gesture-unlocked, not a fake mute label.

Final canonical browser playable-loop check: clicked road at a guest lane, steered vertically, fired with F; score changed 0→1 and the screenshot showed the fed guest's `♥ YUM!` plus colored particle trail while it peeled away. Reload retained count 1 in the separate save namespace.

Canonical `playable/moonlight-munch-run.html` is generated by the build and embeds both used PNGs plus bundled code. It has no external dependencies and preserves the separate `moonlight-munch-run-v1` namespace. The equivalent development `build/index.html` requires its `game.js` and two `build/assets` PNGs.

## Local shooter slice acceptance — 2026-09-17

Updated source/requirements supersede the harmless-bump/manual-toss design above.
16 compiled-export checks pass: three input checks plus legacy migration/storage,
automatic free fire/2D bounds, switch/per-type/rapid retention, multi-hit and
single awards, piercing once-per-guest, caught pickups, supply/restock freeze,
boss warning/attacks/summons, boss hunger checkpoint/reload/completion, burst
cooldown, 20-minute bounded continuation and later side entry/tougher guests.
Strict TypeScript/offline build passes. Build includes generated comic art and
full adapted MIT notice; standalone artifact has no external script/image loads.

Root reviewed both delegated slices and corrected: atlas kind/crop mapping,
road compositing to 80%, long-run scroll continuity, real side-entry positions,
third straight upgrade, delayed first boss attack, hunger dots, food-only
projectiles, 16:9 sizing, compact short-landscape overlays, clickable Resume
card and keyboard focus recovery. Audio promise denial has a safe fallback.

Served in-app browser review: legacy 25 feeds/settings loaded; steering/special
increased feeds, waves advanced into a boss, supplies reached0. Free restock
retained boss hunger 26; pause then reload retained hunger 25. Final build
reloaded that checkpoint. At 844 × 390 pause clock 4.810 stayed 4.810 with F blocked;
Resume button focused canvas and F produced a burst (charge .057), feeding boss.
Actual boss completion advanced stage 1 to 2; no captured warnings/errors.
1280 × 720, 390 × 844 portrait and 844 × 390 landscape reviewed. Portrait start card fits;
landscape HUD and 44px primary controls retained. Viewport override reset.

Chapter House 100 tests and production build pass; exact preview verifier passes
13 previews/19 files across original, snapshot, dist and active 5191 server.
Unknown route 404. Existing large application-chunk advisory remains.
No physical-phone/iPad test, audio listening, full journey balance test or
performance capacity claim. Scenery loop seam remains visible. This is the first
playable slice for Jobe's feedback. Keep local; hosted Site untouched.

Final canonical artifact: 5575531 bytes; SHA-256 B3D91F387E21E99B3C61161C5DF3567FE0701F12C65E45C1EA7079FC29ADA79E.

## Challenge and frame animation acceptance — 2026-09-17

Jobe's follow-up supersedes the first slice's difficulty. The strict build and
25 compiled-export checks pass: nine new hazard checks plus the sixteen retained
mechanic/input/save checks. New checks cover exact one-second automatic/special
suppression without supply or charge loss, food passing over hazards, landing
warning, sustained contact/re-entry, guest and boss drops, road/boss motion,
expiry/restock retention, twenty-minute bounds, offscreen feeding exclusion and
fully upgraded stationary play still encountering hazards.

Tuning: one extra guest per wave, stage-one travel .17 versus .13, initial spawn
interval1.25 versus1.55 seconds, boss hunger50+18 per later stage, projectile
speed.28 versus.24 and attack intervals1.9/1.5/2.2 seconds. Existing crowd cap
seven and free restock remain. Droppers release at0.4–0.6 seconds; stage-three
small guests need two hits. A0.65-second warning precedes contact; hazards expire
at8 seconds and cap8 during travel/4 at boss. Boss drops target current steering
position. Contact alone costs no supplies, and already-flying food keeps moving.

Two transparent generated atlases add four actual source frames per actor and
illustrated potholes/spore pods. Original art is retained. Crops account for
uneven generated rows; no actor bob/squash/rotation substitutes for frame art.
Reduced motion selects stable frame zero. Generation prompts/reference/output
and retained-source choices are in FRAME-AND-HAZARD-PROVENANCE.md. Offline build
embeds four PNGs and the retained MIT notice without external runtime loads.

Browser review first loaded Jobe's stage-six spread/rapid checkpoint, exposing
offscreen instant feeding; source correction is regression-tested. Final play
review used an isolated5192 origin so further QA did not add progress to5191.
Visible potholes/warnings and actual truck/monster frame changes were inspected.
Hazard contact showed KITCHEN JAMMED / FIRE RETURNS IN0.8S, disabled the special
control with charge1.000 retained and supplies50 unchanged; later fireLock0 and
feeds increased. Reduced-motion frame stayed truck0/monster0 as clock advanced.
Pause, portrait390×844 free restock, landscape844×390 start card and help controls
were reviewed. A stale screenshot after viewport resize required reload; DOM
layout and refreshed screenshot agreed. Viewport reset, isolated tab/server
closed. No captured browser errors/warnings. Final application100 tests/build
pass; verifier confirms13 previews/19 exact original/snapshot/dist/served files,
correct MIME and unknown route404. Only Moonlight's artifact hash changed.

Final artifact9742784 bytes; SHA-256
E6C7B2C330679738A8E11D133B384DB94C2E8D2B1AA55C4D9EDB39CD8521A6F2.
No physical-device touch test, audio listening or final balance claim. Four-frame
generated animation is a first pass for Jobe's feedback. Keep local, await next
feedback; no publication, remote push or other game/clubhouse changes.
